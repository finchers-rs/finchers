dist: trusty
sudo: false
language: rust

cache:
  apt: true
  cargo: true
  directories:
    - testcrates/test_edition_2018/target

branches:
  only:
    - master


matrix:
  allow_failures:
    - rust: nightly
  fast_finish: true

  include:
    - rust: stable
      sudo: required
      addons:
        apt:
          packages:
            - libcurl4-openssl-dev
            - libelf-dev
            - libdw-dev
            - binutils-dev
      before_script:
        - rustup component add rustfmt-preview
        - rustup component add clippy-preview
        - cargo install cargo-update || true
        - cargo install cargo-kcov || true
        - cargo install-update -a
        - cargo kcov --print-install-kcov-sh | sh
      script:
        - cargo update
        - cargo test
        - cargo test --all-features
        - cargo test --no-default-features
        - if [[ "${TRAVIS_PULL_REQUEST_BRANCH:-}" = release-* ]]; then cargo publish --dry-run; fi
        - cargo fmt -- --check
        - cargo clippy --tests
      after_success:
        - cargo kcov -v --features "secure tower-web" --coveralls

    - rust: beta
      script:
        - cargo update
        - cargo build
        - (cd testcrates/test_edition_2018; cargo test)

    - rust: nightly
      script:
        - cargo update
        - cargo build
        - cargo test -p doctest

    # The minimal supported toolchain
    - rust: 1.29.1
      script:
        - cargo update
        - cargo build

    - rust: beta
      env: DEPLOY_API_DOC
      script:
        - rm -rf target/doc
        - cargo update
        - cargo doc --no-deps --all-features -p failure
        - cargo doc --no-deps --all-features -p tower-service
        - cargo doc --no-deps --all-features -p tower-web
        - cargo doc --no-deps --all-features
        - rm -f target/doc/.lock
        - echo '<meta http-equiv="refresh" content="0;URL=finchers/index.html">' > target/doc/index.html
      deploy:
        provider: pages
        skip_cleanup: true
        github_token: $GH_TOKEN
        repo: finchers-rs/finchers
        target_branch: gh-pages
        local_dir: target/doc
        on:
          branch: master
