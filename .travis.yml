dist: trusty
sudo: false
language: rust

cache:
  cargo: true
  bundler: true
  directories:
  - doc/target

branches:
  only:
    - master

matrix:
  include:
    - rust: stable
      env: MDBOOK_VERSION="0.1.2"
    - rust: beta
    - rust: nightly
    - rust: nightly-2018-01-31
      env: RUSTFMT_VERSION="0.3.7"

  allow_failures:
    - rust: nightly
    - rust: nightly-2018-01-31

addons:
  apt:
    packages:
      - libcurl4-openssl-dev
      - libelf-dev
      - libdw-dev
      - binutils-dev
      - cmake

before_script:
  - 'if [[ -n $RUSTFMT_VERSION ]]; then bash .travis/install-rustfmt.sh; fi'
  - 'if [[ -n $MDBOOK_VERSION ]]; then bash .travis/install-mdbook.sh; fi'
  - export PATH=$HOME/.local/bin:$PATH

script:
  - bash .travis/script.sh

after_success:
  - bash .travis/after_success.sh
