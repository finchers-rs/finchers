[package]
name = "doctest"
version = "0.0.0"
authors = ["Yusuke Sasaki <yusuke.sasaki.nuem@gmail.com>"]
publish = false

[lib]
name = "doctest"
path = "lib.rs"

[dependencies]
doubter = "0.0.2"

[dev-dependencies]
finchers = { path = "../.." }
